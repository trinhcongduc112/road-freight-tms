# Production deploy config — Makefile auto đọc file này.
# 1 lệnh duy nhất để deploy: make deploy-prod
#
# Đổi domain hoặc fork repo? Sửa 2 dòng dưới + git commit là xong.
# (Không phải secret — DOMAIN public qua DNS, EMAIL public qua git commit author)

# Domain chính (apex). 5 subdomain auto derive: apex, www, route, track, docs
DOMAIN=ductms.id.vn

# Email cho Let's Encrypt — dùng để báo cert sắp hết hạn
EMAIL=ductuyetvoi@gmail.com
